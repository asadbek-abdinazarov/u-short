spring:
  profiles:
    active: prod
  application:
    name: u-short
  jpa:
    open-in-view: off
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html